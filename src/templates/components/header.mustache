<style>

header nav {
    text-align:center;
}

header nav a {
    margin-left:30px;
    margin-right:30px;
    opacity:0.8;
}

header nav a:hover {
    opacity: 1;
}
header .logo {
    width:157px;
}
header .logo {
    top:40px;       
    left:80px;
    position: absolute;
}
.mobile-header .logo {
    width: 120px;
}
header .cta {
    display:block;
    position:absolute;
    top:34px;
    right:80px;
    width:152px;
    height: 40px;
    line-height: 40px;
    text-align:center;
    border-radius: 100px;
    background-color: #00000033;
}

header .cta:hover {
    background-color: #00000066;
}

.leader .cta, .box .cta, .get-started .cta {
    display: inline-block !important;
    height: 60px;
    line-height: 60px;
    text-align:center;
    border-radius: 100px;
    background-color: #FFBC00;
    color:white;
    font-size: 16px;
    font-weight: 600;
    padding-left:35px;
    padding-right:35px;
    box-shadow: 0 20px 40px 0 rgba(0,0,0,0.2);
    animation: glowing-btn-yellow 2s infinite;
}
.leader .cta:hover, .box .cta:hover, .get-started .cta:hover {
    background-color: #f7a001;
}
@keyframes glowing-btn-yellow {
    0% {
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2), 0 0 0 0 rgba(255, 188, 0, 1);
    }
    100% {
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2), 0 0 0 8px rgba(255, 188, 0, 0);
    }
}

@media only screen and (max-width: 820px) {
    header {
        display:none;
    }
    .header-bg {
        display:none;
    }
    .mobile-header {
        display:flex;
    }            
    .save-money .header {
        text-align:center;
        margin-bottom:20px;
    } 
}

</style>


<header>
    <img class="logo" src="/images/logo.png">
    <nav>
        <a href="/">Home</a>
        <a href="/pricing">Pricing</a>
		<a href="/signature-calculator">Signatures</a>
        <a href="https://app.mailtag.io/blog">Blog</a>
    </nav>
    <a href="#" class="cta">Add to Chrome</a>
</header>